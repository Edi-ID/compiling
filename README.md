# compile file python for python3

* How to install the package
```
$ pkg install python
$ pkg install git
$ pip install uncompyle6
$ git clone https://github.com/Edi-ID/compiling.git
$ cd compiling
```

* Usage:
```
$ python main.py
```